# kubernetes/

Manifests base dos workloads da plataforma (o próprio controller/dashboard).
Entregues via GitOps (`gitops/argocd/`), empacotados com Helm
(`helm/automation-controller/`).

> 🗺️ Referência. Requer um cluster EKS provisionado por `terraform/`.
